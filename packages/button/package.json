{
  "name": "button",
  "version": "0.0.0",
  "main": "src/index.js",
  "author": "romain.lebascle@gmail.com",
  "license": "MIT",
  "scripts": {
    "build": "BABEL_ENV=build babel --root-mode upward src --out-dir dist --source-maps --extensions .js,.jsx --delete-dir-on-start --no-comments"
  }
}
